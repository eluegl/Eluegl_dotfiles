super + @space
        rofi -show drun 
 
super + f 
        firefox 
 
super + t 
        kitty 
 
super + c 
        bspc node -c 
 
super + r 
        bspc wm -r 

super + shift + @space
        bspc node -t floating

super + {shift + t,shift +f} 
        bspc node -t {tiled,fullscreen} 
 
super + F1
    bspc desktop -f 1

super + F2
    bspc desktop -f 2

super + F3
    bspc desktop -f 3

super + F4
    bspc desktop -f 4

super + F5
    bspc desktop -f 5

super + F6
    bspc desktop -f 6

super + F7
    bspc desktop -f 7

super + F8
    bspc desktop -f 8

super + F9
    bspc desktop -f 9

super + F10
    bspc desktop -f 10


super + shift + F1
    bspc node -d 1

super + shift + F2
    bspc node -d 2

super + shift + F3
    bspc node -d 3

super + shift + F4
    bspc node -d 4

super + shift + F5
    bspc node -d 5

super + shift + F6
    bspc node -d 6

super + shift + F7
    bspc node -d 7

super + shift + F8
    bspc node -d 8

super + shift + F9
    bspc node -d 9

super + shift + F10
    bspc node -d 10
  
super + l 
        slock 
 
super + Print 
        scrot -s -F ~/img/screenshots/%Y-%m-%d-%H%M%S.png 
 
super + ctrl + f 
        flatpak run dev.goats.xivlauncher 
 
super + Left 
        bspc desktop -f prev 
 
super + Right 
        bspc desktop -f next 
 
XF86AudioRaiseVolume 
    wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
    notify-send "Volume ↑" "$(wpctl get-volume @DEFAULT_AUDIO_SINK@)" 
 
XF86AudioLowerVolume 
    wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
    notify-send "Volume ↓" "$(wpctl get-volume @DEFAULT_AUDIO_SINK@)"
 
XF86AudioMute 
    wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
    notify-send "Volume" "$(wpctl get-volume @DEFAULT_AUDIO_SINK@)"
 
XF86AudioPlay 
    playerctl play-pause 
 
XF86AudioNext 
    playerctl next 
 
XF86AudioPrev 
    playerctl previous

super + ctrl + alt + l
    loginctl terminate-user $USER
